<!-- <view class="container {{successFlg ? 'successsSty':''}}">
    <view class="header-main" wx:if="{{!successFlg}}">
      <view class="header-mainItem"></view>
      <view class="header-mainItem" bindtap="challengePage">我的活动</view>
      <view class="header-mainItem" bindtap="myfindPage">健康资讯</view>
    </view>
    <collectionOption  wx:if="{{successFlg}}"  id="filterCmp"  bind:parentReceive="parentCallBack" />
</view>
<footer active="{{active}}"  wx:if="{{!successFlg}}"></footer> -->

<view class="container {{successFlg ? 'successsSty':''}}">
    <view class="top-box">
      <view class="back-icon" bindtap="navigateToStep"></view>
      <view class="active-title">
        <view class="item" bindtap="challengePage"></view>
        <view class="item" bindtap="myfindPage"></view>
      </view>
      <view class="hot-active" catchtap="challengePage"></view>
      <image class="top" src="../../image/top.png"></image>
    </view>
    <view class="bottom-box">
      <view class="health-more" catchtap="myfindPage"></view>
      <image class="bottom" src="../../image/bottom.png"></image>
    </view>
</view>
<footer active="{{active}}"  wx:if="{{!successFlg}}"></footer>

